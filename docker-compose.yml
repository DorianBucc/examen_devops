services:

  backend:
    build: ./backend
    container_name: arcade-backend

    ports:
      - "${FLASK_PORT}:5000"

    environment:
      DATABASE_PATH: ${DATABASE_PATH}
      FLASK_ENV: ${FLASK_ENV}

    volumes:
      - sqlite_data:/app/data

volumes:
  sqlite_data: